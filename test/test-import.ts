import CronScheduler, {
  createScheduler,
  jobTemplates,
} from "../src/scheduler/cron.js";
console.log("CronScheduler:", typeof CronScheduler);
console.log("createScheduler:", typeof createScheduler);
console.log("jobTemplates:", typeof jobTemplates);
